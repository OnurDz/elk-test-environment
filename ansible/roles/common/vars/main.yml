---
common:
  apt:
    packages:
      - dirmngr
      - apt-transport-https
      - lsb-release
      - ca-certificates
      - python-pip
      - python-apt
      - aptitude
      - curl
      - software-properties-common

  file:
    ansible_path: '$HOME/ansible-files'